import { TextArea } from 'grommet/components';
import styled from 'styled-components';

const StyledTextArea = styled(TextArea)`
  flex: 1 1 44vh;
  border: none;
  border-radius: 0;
`;

export default StyledTextArea;
